<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tkzou</groupId>
        <artifactId>forum-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>demo-runner</artifactId>
    <name>demo-runner</name>
    <description>演示启动器：聚合 admin + recommend 为一个可执行 Spring Boot 应用（单进程、零中间件），含 static/yml/DataStore</description>

    <dependencies>
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-admin-server</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-recommend-server</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- 集成测试用到 OfflineEvaluator（离线评估），仅 test scope，不进演示 jar -->
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-offline-job</artifactId>
            <version>${project.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

    <!-- 生产 profile：引入 JDBC/MySQL + Kafka/Redis 运行时依赖，并编译 src/prod/java
         （MySqlDataStore 持久化 + MySqlOutboxStore 事件必达；kafka-clients/jedis 为运行时必需，非 optional） -->
    <profiles>
        <profile>
            <id>prod</id>
            <dependencies>
                <dependency>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-starter-jdbc</artifactId>
                </dependency>
                <dependency>
                    <groupId>com.mysql</groupId>
                    <artifactId>mysql-connector-j</artifactId>
                    <scope>runtime</scope>
                </dependency>
                <!-- prod 运行时：KafkaPostCreatedProducer/Consumer（kafka-clients）与 RedisFollowFeedStore 等（jedis），
                     上游模块里是 optional 不传递，这里显式补上 -->
                <dependency>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                </dependency>
                <dependency>
                    <groupId>redis.clients</groupId>
                    <artifactId>jedis</artifactId>
                </dependency>
                <!-- ClickHouse 行为日志读取（近线/离线分析） -->
                <dependency>
                    <groupId>com.clickhouse</groupId>
                    <artifactId>clickhouse-jdbc</artifactId>
                    <version>0.4.6</version>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.4.0</version>
                        <executions>
                            <execution>
                                <id>add-prod-source</id>
                                <phase>generate-sources</phase>
                                <goals><goal>add-source</goal></goals>
                                <configuration>
                                    <sources><source>src/prod/java</source></sources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
