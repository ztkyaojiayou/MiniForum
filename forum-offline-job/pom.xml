<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.tkzou</groupId>
        <artifactId>forum-parent</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>forum-offline-job</artifactId>
    <name>forum-offline-job</name>
    <description>离线层：定时离线评估（OfflineEvalScheduler / OfflineEvaluator / 指标 / 时间切分），独立运行器</description>

    <dependencies>
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-recommend-server</artifactId>
            <version>${project.version}</version>
        </dependency>
        <!-- spring-context + 定时任务 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <!-- 离线评估从 ClickHouse 读行为全量（生产形态） -->
        <dependency>
            <groupId>com.clickhouse</groupId>
            <artifactId>clickhouse-jdbc</artifactId>
            <version>0.4.6</version>
        </dependency>

        <dependency>
            <groupId>com.tkzou</groupId>
            <artifactId>forum-core</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
